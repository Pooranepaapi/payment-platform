package org.personal.enums;

public enum PaymentType {
    // UPI types
    RBLUPI,
    HDFCUPI,
    KOTAKUPI,
    DYNAMIC_QR,

    // Card types (future)
    HDFCCC,
    RBLCC,

    // NetBanking types (future)
    HDFCNB,
    ABORBNB
}
